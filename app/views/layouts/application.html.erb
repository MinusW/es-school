<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Es School" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <% if current_page?(root_path) %>
      <%= yield %>
    <% else %>
      <header class="bg-blue-800 text-white shadow-md">
        <div class="container mx-auto px-4 py-3 flex items-center justify-between">
          <a href="<%= root_path %>" class="text-xl font-bold">ES School</a>
          <nav>
            <ul class="flex space-x-4">
              <% if user_signed_in? %>
                <li><a href="<%= classrooms_path %>" class="hover:text-blue-200">Classrooms</a></li>
                <% if current_user.dean? || current_user.teacher? %>
                  <li><a href="<%= students_path %>" class="hover:text-blue-200">Students</a></li>
                <% end %>
                <% if current_user.dean? %>
                  <li><a href="<%= users_path %>" class="hover:text-blue-200">Users</a></li>
                <% end %>
                <li><a href="<%= destroy_user_session_path %>" data-turbo-method="delete" class="hover:text-blue-200">Sign Out</a></li>
              <% else %>
                <li><a href="<%= new_user_session_path %>" class="hover:text-blue-200">Sign In</a></li>
                <li><a href="<%= new_user_registration_path %>" class="hover:text-blue-200">Sign Up</a></li>
              <% end %>
            </ul>
          </nav>
        </div>
      </header>
      
      <main class="container mx-auto px-4 py-6">
        <% if notice %>
          <div class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4 mb-4" role="alert">
            <p><%= notice %></p>
          </div>
        <% end %>
        
        <% if alert %>
          <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-4" role="alert">
            <p><%= alert %></p>
          </div>
        <% end %>
        
        <%= yield %>
      </main>
    <% end %>
  </body>
</html>
