<%= form_with(model: theme) do |form| %>
  <% if theme.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(theme.errors.count, "error") %> prohibited this theme from being saved:</h2>

      <ul>
        <% theme.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :module_name, style: "display: block" %>
    <%= form.text_field :module_name %>
  </div>

  <div>
    <%= form.label :module_description, style: "display: block" %>
    <%= form.text_area :module_description %>
  </div>

  <div>
    <%= form.label :is_archived, style: "display: block" %>
    <%= form.check_box :is_archived %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
