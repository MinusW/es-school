<%= form_with(model: classroom) do |form| %>
  <% if classroom.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
        <% classroom.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :class_type_id, style: "display: block" %>
    <%= form.number_field :class_type_id %>
  </div>

  <div>
    <%= form.label :room_id, style: "display: block" %>
    <%= form.number_field :room_id %>
  </div>

  <div>
    <%= form.label :teacher_id, style: "display: block" %>
    <%= form.number_field :teacher_id %>
  </div>

  <div>
    <%= form.label :is_archived, style: "display: block" %>
    <%= form.checkbox :is_archived %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
